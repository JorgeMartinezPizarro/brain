# WIP: add the code for the commands. 

start:
	# load the images and start the containers
stop:
	# stop the containers
load:
	# load the state of the system at a single point (in case of failures, for example)
save:	
	# save the state of the system (volumes) to a single docker file to your own docker registry
